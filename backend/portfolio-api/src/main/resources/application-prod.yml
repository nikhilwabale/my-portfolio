# Activated on Render via SPRING_PROFILES_ACTIVE=prod.
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

spring:
  jpa:
    show-sql: false

logging:
  level:
    root: WARN
    com.nikhilwabale.portfolioapi: INFO
